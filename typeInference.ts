
let tech='MERN';
// tech=15; this will throw error as above it has inferenced as string;
console.log(tech + ' and the type is ' + typeof tech);

let exp=2;
console.log(exp + ' and the type is ' + typeof exp);
// exp='2';  this will throw error as above it has inferenced as number;

let ishard=true
console.log(ishard + ' and the type is ' + typeof ishard);

// ishard='true';  this will throw error as above it has inferenced as boolean.

